<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_contract_history_toggle_form" model="ir.ui.view">
        <field name="name">hr.contract.history.toggle.form</field>
        <field name="model">hr.contract.history</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_history_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="inside">
                <field name="signed_by_employer" widget="boolean_toggle" readonly="True"/>
            </xpath>
        </field>
    </record>

</odoo>
