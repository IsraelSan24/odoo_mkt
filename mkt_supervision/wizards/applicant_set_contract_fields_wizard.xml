<odoo>
  <record id="view_applicant_approval_wizard_form" model="ir.ui.view">
    <field name="name">applicant.set.contract.fields.wizard.form</field>
    <field name="model">applicant.set.contract.fields.wizard</field>
    <field name="arch" type="xml">
      <form string="Set First Contract Fields">
        <group>
          <field name="cost_center_id" string= "Centro de Costo" options="{'no_create': True}"/>
          <field name="first_contract_start" string= "Fecha Inicio de Contrato"/>
          <field name="first_contract_end" string= "Fecha Fin de Contrato"/>
          <field name="salary_proposed" string= "Salario"/>
          <field name="work_type" string= "Condición"/>
          <field name="parent_id" string= "Jefe Directo" options="{'no_create': True, 'no_edit': True}"/>
          <field name="company_id" invisible='1'/>
        </group>
        <footer>
          <button string="Save and Approve" type="object" name="action_save_and_approve" class="btn-primary"/>
          <button string="Save" type="object" name="action_save" class="btn-secondary"/>
          <button string="Cancel" special="cancel" class="btn-secondary"/>
        </footer>
      </form>
    </field>
  </record>
</odoo>
